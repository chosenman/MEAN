<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Add new</h1>
    <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
         <h3 style="color:red"><%= errors[x].message %></h3>
        <% } %>
    <% } %>


<br>
<br>
    <a href="/"><< GO back</a>
  </body>
</html>
